workflows:
  android-build:
    name: Build APK
    environment:
      flutter: stable

    scripts:
      - cd flutter/samples/experimental/android_splash_screen
      - flutter pub get
      - flutter build apk --release

    artifacts:
      - flutter/samples/experimental/android_splash_screen/build/app/outputs/flutter-apk/app-release.apk

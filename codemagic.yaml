workflows:
  android-apk-build:
    name: Build APK
    environment:
      flutter: stable

    scripts:
      - cd android_splash_screen
      - ls
      - cd android_splash_screen
      - ls
      - flutter pub get
      - flutter build apk --release

    artifacts:
      - android_splash_screen/android_splash_screen/build/app/outputs/flutter-apk/app-release.apk
